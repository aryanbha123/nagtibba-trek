/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env*
.vercel
next-env.d.ts

# Unused large PNG source duplicates — the site serves the optimised .webp
# versions. Kept locally but excluded from the repo/deploy.
/public/assets/*.png
